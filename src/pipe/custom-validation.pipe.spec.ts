import { CustomValidationPipe } from './custom-validation.pipe';

describe('Custom Validation Pipe', () => {
  it('should be defined', () => {
    expect(new CustomValidationPipe()).toBeDefined();
  });
});
